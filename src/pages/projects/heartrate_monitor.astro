---
import BaseLayout from "../../layouts/BaseLayout.astro";
---

<BaseLayout>
  <div class="max-w-3xl mx-auto py-12">
    <h1 class="text-4xl font-bold mb-4">
      PPG Heart Rate Sensor
    </h1>

   <div class="flex justify-between items-center mb-8">
      <p class="text-lg text-gray-600">
        Electrical Lead
      </p>
      
      <p class="text-lg text-gray-600">
        Jan 2026 - Mar 2026
      </p>
</div>

    <div class="space-y-6">

      <div>
        <h2 class="text-2xl font-semibold">Overview</h2>
        <p>
          Designed and prototyped a photoplethysmography (PPG) heart rate sensor using an
          infrared emitter (940 nm) and photodiode to detect blood volume changes in tissue for measuring heart rate.
          The system converts very small photodiode currents into a filtered analog
          waveform representing the cardiac cycle, which is then digitized using a
          Schmitt-trigger comparator to produce heart-rate pulses.
        </p>
      </div>

      <div>
        <h2 class="text-2xl font-semibold">System Architecture</h2>
        <p>
          I designed a multi-stage analog signal chain that converts the photodiode current
          into a clean pulse waveform suitable for digital processing.
          (INSERT CIRCUIT IMAGE)
          
          The design was first simulated in LTSpice
          to verify the response before physical prototype on a breadboard.
          (INSERT CIRCUIT SIMULATION)
        </p>
      </div>

      <div>
        <h2 class="text-2xl font-semibold">Signal Chain</h2>

        <p class="mb-4">
          The circuit consists of three main analog stages followed by a digital pulse output.
        </p>

        <ul class="list-disc ml-6 space-y-2">
          <li>
            <b>Stage 1 - Transimpedance Amplifier</b><br/>
            Converts photodiode current (~1 µA DC with ~0.1 µA AC variation)
            into a measurable voltage using an op-amp with a high feedback resistance.
            (INSERT IMAGE of OUTPUT)
          </li>

          <li>
            <b>Stage 2 - Bandpass Filtering and Gain</b><br/>
            A high-pass filter removes the DC offset caused by constant tissue reflection,
            while a low-pass filter suppresses high-frequency noise.
            This creates a bandpass response tuned to the physiological heart rate range.
            (INSERT IMAGE of OUTPUT)
          </li>

          <li>
            <b>Stage 3 - Comparator (Schmitt Trigger)</b><br/>
            The analog waveform is converted into a digital pulse using a hysteretic
            comparator, ensuring robust switching even in the presence of noise.
            (INSERT IMAGE of OUTPUT)
          </li>
        </ul>
      </div>

      <div>
        <h2 class="text-2xl font-semibold">Impact</h2>
        <p>
          The system produces a clean pulse signal corresponding to each heartbeat.
          This architecture demonstrates how extremely small biosignals can be extracted
          using careful analog circuit design and signal conditioning. Smartwatches (like Apple Watch) use similar conceptual techniques to measure heart rate optically as well.
        </p>
      </div>

    </div>
  </div>
</BaseLayout>